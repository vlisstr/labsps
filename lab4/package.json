{
  "name": "lab4-async-events",
  "version": "2.0.0",
  "description": "Lab 4: sync vs async communication via Integration Events",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:sqlite": "PERSISTENCE=sqlite node server.js",
    "dev": "nodemon server.js",
    "test": "node --test --test-reporter=spec 'tests/**/*.test.js'",
    "test:unit": "node --test --test-reporter=spec 'tests/unit/**/*.test.js'",
    "test:integration": "node --test --test-reporter=spec 'tests/integration/**/*.test.js'"
  },
  "keywords": ["ddd", "cqs", "events", "pub-sub", "layered-architecture", "express", "jwt"],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
